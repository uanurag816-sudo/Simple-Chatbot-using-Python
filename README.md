# Simple-Chatbot-using-Python
# 🤖 Simple Chatbot  A basic chatbot built using Python that responds to predefined user messages. It uses simple `if-else` conditions to interact with the user through the command line. The chatbot continues the conversation until the user types **"bye"** to exit.  
